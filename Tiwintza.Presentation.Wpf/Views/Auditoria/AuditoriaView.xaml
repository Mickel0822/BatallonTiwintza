<UserControl x:Class="Tiwintza.Presentation.Wpf.Views.Auditoria.AuditoriaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="720"
             d:DesignWidth="1200">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>

        <Style x:Key="FilterLabel" TargetType="TextBlock" BasedOn="{StaticResource Caption}">
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>

        <Style x:Key="AccionChip" TargetType="Border">
            <Setter Property="Padding" Value="10,4"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Background" Value="{StaticResource StatusInfoBrush}"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Accion}" Value="Creacion">
                    <Setter Property="Background" Value="{StaticResource StatusSuccessBrush}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Accion}" Value="Actualizacion">
                    <Setter Property="Background" Value="{StaticResource StatusInfoBrush}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Accion}" Value="Baja">
                    <Setter Property="Background" Value="{StaticResource StatusDangerBrush}"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="AuditoriaDataGrid" TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="RowHeight" Value="48"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="24,16,24,24">
            <TextBlock Text="Auditoria" Style="{StaticResource H1}"/>
            <TextBlock Text="Consulte el historial de acciones realizadas en el sistema" Style="{StaticResource Caption}"/>
        </StackPanel>

        <Border Grid.Row="1" Margin="24,0,24,24" Style="{StaticResource Card}" Padding="24">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,12,12">
                    <TextBlock Text="Fecha inicio" Style="{StaticResource FilterLabel}"/>
                    <DatePicker SelectedDate="{Binding FechaInicio}"/>
                </StackPanel>
                <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,12,12">
                    <TextBlock Text="Fecha fin" Style="{StaticResource FilterLabel}"/>
                    <DatePicker SelectedDate="{Binding FechaFin}"/>
                </StackPanel>
                <StackPanel Grid.Row="0" Grid.Column="2" Margin="0,0,12,12">
                    <TextBlock Text="Usuario" Style="{StaticResource FilterLabel}"/>
                    <ComboBox ItemsSource="{Binding Usuarios}" SelectedItem="{Binding UsuarioSeleccionado}"/>
                </StackPanel>
                <StackPanel Grid.Row="0" Grid.Column="3" Margin="0,0,0,12">
                    <TextBlock Text="Entidad" Style="{StaticResource FilterLabel}"/>
                    <ComboBox ItemsSource="{Binding Entidades}" SelectedItem="{Binding EntidadSeleccionada}"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,12,0">
                    <TextBlock Text="Accion" Style="{StaticResource FilterLabel}"/>
                    <ComboBox ItemsSource="{Binding Acciones}" SelectedItem="{Binding AccionSeleccionada}"/>
                </StackPanel>
                <StackPanel Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Margin="0,0,12,0">
                    <TextBlock Text="Buscar" Style="{StaticResource FilterLabel}"/>
                    <TextBox Text="{Binding Texto, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBox}" Tag="ID o resumen"/>
                </StackPanel>
                <StackPanel Grid.Row="1" Grid.Column="3" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                    <Button Content="Filtrar" Style="{StaticResource PrimaryButton}" Command="{Binding BuscarCommand}" Margin="0,0,8,0"/>
                    <Button Content="Limpiar" Style="{StaticResource OutlineButton}" Command="{Binding LimpiarFiltrosCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="2" Margin="24,0,24,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource Card}" Padding="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <DockPanel Grid.Row="0" Margin="24,24,24,12">
                        <StackPanel Orientation="Vertical" DockPanel.Dock="Left">
                            <TextBlock Text="Registros" Style="{StaticResource H3}"/>
                            <TextBlock Text="Auditoria de eventos recientes" Style="{StaticResource Caption}"/>
                        </StackPanel>
                        <Button Content="Exportar" Style="{StaticResource OutlineButton}" Command="{Binding ExportarCommand}" HorizontalAlignment="Right"/>
                    </DockPanel>

                    <DataGrid Grid.Row="1"
                              ItemsSource="{Binding Items}"
                              Sorting="OnSorting"
                              SelectedItem="{Binding Seleccionado, Mode=TwoWay}"
                              Style="{StaticResource AuditoriaDataGrid}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Fecha" Binding="{Binding FechaTexto}" Width="180" SortMemberPath="fecha"/>
                            <DataGridTextColumn Header="Usuario" Binding="{Binding Usuario}" Width="160" SortMemberPath="usuario"/>
                            <DataGridTemplateColumn Header="Accion" Width="140" SortMemberPath="accion">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource AccionChip}">
                                            <TextBlock Text="{Binding Accion}" Style="{StaticResource Caption}"/>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="Entidad" Binding="{Binding Entidad}" Width="160" SortMemberPath="entidad"/>
                            <DataGridTextColumn Header="ID" Binding="{Binding EntidadId}" Width="100" SortMemberPath="id"/>
                            <DataGridTextColumn Header="Resumen" Binding="{Binding Resumen}" Width="*" SortMemberPath="resumen"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource Card}" Padding="24" Margin="16,0,0,0">
                <StackPanel>
                    <TextBlock Text="Detalle" Style="{StaticResource H3}"/>
                    <TextBlock Text="{Binding Seleccionado.Resumen}" Style="{StaticResource Body}" Margin="0,8,0,12"/>
                    <Border Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource GridBorderBrush}" BorderThickness="1" CornerRadius="12">
                        <ScrollViewer Height="260">
                            <TextBox Text="{Binding DetalleFormateado}" FontFamily="Consolas" FontSize="12" TextWrapping="Wrap" Background="Transparent" BorderThickness="0" IsReadOnly="True" AcceptsReturn="True"/>
                        </ScrollViewer>
                    </Border>
                </StackPanel>
            </Border>
        </Grid>

        <DockPanel Grid.Row="3" Margin="24,0,24,24" LastChildFill="False">
            <TextBlock Text="{Binding Page}" Style="{StaticResource Caption}" VerticalAlignment="Center" Margin="0,0,16,0"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Right">
                <Button Content="Anterior" Style="{StaticResource OutlineButton}" Command="{Binding PaginaAnteriorCommand}" Margin="0,0,8,0"/>
                <Button Content="Siguiente" Style="{StaticResource OutlineButton}" Command="{Binding PaginaSiguienteCommand}"/>
            </StackPanel>
            <TextBlock Text="{Binding Total, StringFormat='Total: {0} registros'}" Style="{StaticResource Caption}" VerticalAlignment="Center"/>
        </DockPanel>

        <!-- Notificacion -->
        <Grid Panel.ZIndex="20" Grid.RowSpan="4">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsNotifyOpen}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            <Rectangle Fill="#80000000"/>
            <Border Width="420" Padding="24" Background="White" CornerRadius="12" HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <iconPacks:PackIconMaterial Width="28" Height="28">
                            <iconPacks:PackIconMaterial.Style>
                                <Style TargetType="iconPacks:PackIconMaterial">
                                    <Setter Property="Kind" Value="CheckCircleOutline"/>
                                    <Setter Property="Foreground" Value="{DynamicResource SuccessBrush}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding NotifyIsError}" Value="True">
                                            <Setter Property="Kind" Value="AlertCircleOutline"/>
                                            <Setter Property="Foreground" Value="{DynamicResource ErrorBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </iconPacks:PackIconMaterial.Style>
                        </iconPacks:PackIconMaterial>
                        <TextBlock Text="{Binding NotifyTitle}" Margin="8,0,0,0">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource H3}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding NotifyIsError}" Value="True">
                                            <Setter Property="Foreground" Value="{DynamicResource ErrorBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                    <TextBlock Text="{Binding NotifyMessage}" Style="{StaticResource Body}"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                        <Button Content="Aceptar" Command="{Binding CerrarNotificacionCommand}" Style="{StaticResource PrimaryButton}" Padding="16,6"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>

    </Grid>
</UserControl>




