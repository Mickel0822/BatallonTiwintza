<UserControl x:Class="Tiwintza.Presentation.Wpf.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" xmlns:sys="clr-namespace:System;assembly=mscorlib"
             d:DesignHeight="720"
             d:DesignWidth="1200">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>

        <Style x:Key="ResumenCardTitle" TargetType="TextBlock" BasedOn="{StaticResource Caption}">
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>

        <Style x:Key="ResumenCardValue" TargetType="TextBlock" BasedOn="{StaticResource H2}">
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="ResumenVariation" TargetType="TextBlock" BasedOn="{StaticResource Caption}">
            <Setter Property="Foreground" Value="{StaticResource MutedBrush}"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsPositive}" Value="True">
                    <Setter Property="Foreground" Value="{StaticResource SuccessBrush}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding IsPositive}" Value="False">
                    <Setter Property="Foreground" Value="{StaticResource ErrorBrush}"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ChipBorderStyle" TargetType="Border">
            <Setter Property="Padding" Value="10,4"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Background" Value="{StaticResource StatusInfoBrush}"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Tipo}" Value="Ingreso">
                    <Setter Property="Background" Value="{StaticResource StatusSuccessBrush}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Tipo}" Value="Salida">
                    <Setter Property="Background" Value="{StaticResource StatusInfoBrush}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Tipo}" Value="Traslado">
                    <Setter Property="Background" Value="{StaticResource StatusWarningBrush}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Tipo}" Value="Baja">
                    <Setter Property="Background" Value="{StaticResource StatusDangerBrush}"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="QuickActionButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="{StaticResource SurfaceAltBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource OnSurfaceBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="16"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MovimientosDataGrid" TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowBackground" Value="Transparent"/>
            <Setter Property="AlternatingRowBackground" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="RowHeight" Value="48"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="24,16,24,24">
            <TextBlock Text="Dashboard" Style="{StaticResource H1}"/>
            <TextBlock Text="Bienvenido al sistema de control de activos e inventario" Style="{StaticResource Caption}"/>
        </StackPanel>

        <!-- KPIs -->
        <UniformGrid Grid.Row="1" Columns="3" Margin="24,0,24,24" HorizontalAlignment="Stretch" VerticalAlignment="Top" Rows="1">
            <Border Style="{StaticResource Card}" Margin="0,0,16,0">
                <Grid DataContext="{Binding KpiActivos}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,12">
                        <Border Background="{StaticResource StatusInfoBrush}" CornerRadius="12" Padding="10">
                            <iconPacks:PackIconMaterial Kind="TrendingUp" Width="24" Height="24"/>
                        </Border>
                        <TextBlock Text="Activos operativos" Style="{StaticResource ResumenCardTitle}" Margin="12,6,0,0"/>
                    </StackPanel>

                    <TextBlock Grid.Row="1" Text="{Binding Actual}" Style="{StaticResource ResumenCardValue}"/>
                    <TextBlock Grid.Row="2" Text="{Binding VariationText}" Style="{StaticResource ResumenVariation}"/>
                </Grid>
            </Border>

            <Border Style="{StaticResource Card}" Margin="0,0,16,0">
                <Grid DataContext="{Binding KpiCriticos}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,12">
                        <Border Background="{StaticResource StatusWarningBrush}" CornerRadius="12" Padding="10">
                            <iconPacks:PackIconMaterial Kind="AlertOutline" Width="24" Height="24"/>
                        </Border>
                        <TextBlock Text="Items en critico" Style="{StaticResource ResumenCardTitle}" Margin="12,6,0,0"/>
                    </StackPanel>

                    <TextBlock Grid.Row="1" Text="{Binding Actual}" Style="{StaticResource ResumenCardValue}"/>
                    <TextBlock Grid.Row="2" Text="{Binding VariationText}" Style="{StaticResource ResumenVariation}"/>
                </Grid>
            </Border>

            <Border Style="{StaticResource Card}">
                <Grid DataContext="{Binding KpiBajas}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,12">
                        <Border Background="{StaticResource StatusDangerBrush}" CornerRadius="12" Padding="10">
                            <iconPacks:PackIconMaterial Kind="ArrowDownBoldCircleOutline" Width="24" Height="24"/>
                        </Border>
                        <TextBlock Text="Bajas del mes" Style="{StaticResource ResumenCardTitle}" Margin="12,6,0,0"/>
                    </StackPanel>

                    <TextBlock Grid.Row="1" Text="{Binding Actual}" Style="{StaticResource ResumenCardValue}"/>
                    <TextBlock Grid.Row="2" Text="{Binding VariationText}" Style="{StaticResource ResumenVariation}"/>
                </Grid>
            </Border>
        </UniformGrid>

        <!-- Movements table -->
        <Border Grid.Row="2" Margin="24,0" Style="{StaticResource Card}" Padding="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <DockPanel Grid.Row="0" Margin="24,24,24,12">
                    <StackPanel Orientation="Vertical" DockPanel.Dock="Left">
                        <TextBlock Text="Ultimos movimientos" Style="{StaticResource H3}"/>
                        <TextBlock Text="Resumen de las operaciones mas recientes" Style="{StaticResource Caption}"/>
                    </StackPanel>
                    <Button Content="Exportar" Command="{Binding ExportarMovimientosCommand}" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Right"/>
                </DockPanel>

                <DataGrid Grid.Row="1"
                          ItemsSource="{Binding Movimientos}"
                          Style="{StaticResource MovimientosDataGrid}"
                          Margin="24,0,24,24">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Codigo}" Width="120"/>
                        <DataGridTextColumn Header="Fecha" Binding="{Binding FechaTexto}" Width="150"/>
                        <DataGridTemplateColumn Header="Tipo" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource ChipBorderStyle}">
                                        <TextBlock Text="{Binding TipoTexto}" Style="{StaticResource Caption}"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Descripcion" Binding="{Binding Descripcion}" Width="*"/>
                        <DataGridTextColumn Header="Usuario" Binding="{Binding Usuario}" Width="160"/>
                        <DataGridTextColumn Header="Items" Binding="{Binding Items}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Quick actions -->
        <UniformGrid Grid.Row="3" Columns="3" Margin="24,32,24,24" HorizontalAlignment="Stretch">
            <Button Style="{StaticResource QuickActionButton}" Command="{Binding RegistrarActivoCommand}" Margin="0,0,16,0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Background="{StaticResource StatusSuccessBrush}" CornerRadius="12" Padding="12">
                        <iconPacks:PackIconMaterial Kind="PlusCircleOutline" Width="24" Height="24"/>
                    </Border>
                    <StackPanel Margin="12,0,0,0">
                        <TextBlock Text="Registrar activo" Style="{StaticResource H4}"/>
                        <TextBlock Text="Agregar un nuevo activo al inventario" Style="{StaticResource Caption}"/>
                    </StackPanel>
                </StackPanel>
            </Button>

            <Button Style="{StaticResource QuickActionButton}" Command="{Binding RegistrarIngresoCommand}" Margin="0,0,16,0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Background="{StaticResource StatusInfoBrush}" CornerRadius="12" Padding="12">
                        <iconPacks:PackIconMaterial Kind="PackageVariantClosed" Width="24" Height="24"/>
                    </Border>
                    <StackPanel Margin="12,0,0,0">
                        <TextBlock Text="Registrar Existencia" Style="{StaticResource H4}"/>
                        <TextBlock Text="Gestione el inventario de suministros" Style="{StaticResource Caption}"/>
                    </StackPanel>
                </StackPanel>
            </Button>

            <Button Style="{StaticResource QuickActionButton}" Command="{Binding MostrarDialogoReporteAreaCommand}">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Background="{StaticResource StatusWarningBrush}" CornerRadius="12" Padding="12">
                        <iconPacks:PackIconMaterial Kind="FileChartOutline" Width="24" Height="24"/>
                    </Border>
                    <StackPanel Margin="12,0,0,0">
                        <TextBlock Text="Reporte por area" Style="{StaticResource H4}"/>
                        <TextBlock Text="Exportar activos por area" Style="{StaticResource Caption}"/>
                    </StackPanel>
                </StackPanel>
            </Button>
        </UniformGrid>

        <!-- Dialogo de reporte por area -->
        <Grid Panel.ZIndex="10" Grid.RowSpan="4">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsAreaDialogOpen}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            <Rectangle Fill="#80000000"/>
            <Border Width="420" Padding="24" Background="White" CornerRadius="18" HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel>
                    <TextBlock Text="Reporte por area" Style="{StaticResource H3}"/>
                    <TextBlock Text="Seleccione el area para generar el reporte" Style="{StaticResource Caption}" Margin="0,4,0,12"/>

                    <ComboBox ItemsSource="{Binding Areas}"
                              DisplayMemberPath="Nombre"
                              SelectedValuePath="Id"
                              SelectedValue="{Binding AreaSeleccionadaId, Mode=TwoWay}">
                    </ComboBox>

                    <TextBlock Text="{Binding AreaDialogMessage}" Foreground="{StaticResource ErrorBrush}" Margin="0,8,0,0">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource Caption}">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding AreaDialogMessage}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding AreaDialogMessage}" Value="">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                        <Button Content="Cancelar" Command="{Binding CancelarReporteAreaCommand}" Style="{StaticResource QuickActionButton}" Background="{StaticResource SurfaceBrush}" Foreground="{StaticResource OnSurfaceBrush}" Padding="12,6" Margin="0,0,8,0"/>
                        <Button Content="Generar" Command="{Binding GenerarReporteAreaCommand}" Style="{StaticResource PrimaryButton}" Padding="16,6"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Notificacion -->
        <Grid Panel.ZIndex="20" Grid.RowSpan="4">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsNotifyOpen}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            <Rectangle Fill="#80000000"/>
            <Border Width="420" Padding="24" Background="White" CornerRadius="18" HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel>
                    <TextBlock Text="{Binding NotifyTitle}" Style="{StaticResource H3}"/>
                    <TextBlock Text="{Binding NotifyMessage}" Style="{StaticResource Body}" Margin="0,8,0,0"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                        <Button Content="Aceptar" Command="{Binding CerrarNotificacionCommand}" Style="{StaticResource PrimaryButton}" Padding="16,6"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>



