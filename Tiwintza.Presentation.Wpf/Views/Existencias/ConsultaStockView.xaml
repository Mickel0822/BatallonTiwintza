<UserControl x:Class="Tiwintza.Presentation.Wpf.Views.Existencias.ConsultaStockView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1000">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        
        <Style x:Key="StockRowStyle" TargetType="DataGridRow">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#EEE"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding NivelEstado}" Value="Critico">
                    <Setter Property="Background" Value="#FFFCE8E6"/> <!-- Red background for critical -->
                    <Setter Property="Foreground" Value="#C62828"/>
                </DataTrigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F5F5F5"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Existencias / Consulta" Style="{StaticResource Caption}" Foreground="{StaticResource MutedBrush}"/>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Consulta de Stock" Style="{StaticResource H1}"/>
                <iconPacks:PackIconMaterial Kind="Magnify" Width="24" Height="24" Margin="12,0,0,0" VerticalAlignment="Center" Foreground="{StaticResource PrimaryBrush}"/>
            </StackPanel>
            <TextBlock Text="Monitoreo de niveles de inventario y alertas de reabastecimiento" Style="{StaticResource Caption}"/>
        </StackPanel>

        <!-- Filtros -->
        <Border Grid.Row="1" Style="{StaticResource Card}" Padding="16" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" 
                         Text="{Binding TextoBusqueda, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource InputTextBox}"
                         Tag="Buscar productos..."
                         Margin="0,0,16,0">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Enter" Command="{Binding BuscarCommand}"/>
                    </TextBox.InputBindings>
                </TextBox>

                <ToggleButton Grid.Column="1" 
                              IsChecked="{Binding SoloCriticos}"
                              Margin="0,0,16,0"
                              Padding="12,8"
                              Cursor="Hand">
                    <ToggleButton.Style>
                        <Style TargetType="ToggleButton">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                            <Setter Property="BorderThickness" Value="1.5"/>
                            <Setter Property="Foreground" Value="{DynamicResource PrimaryBrush}"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="Bg" 
                                                Background="{TemplateBinding Background}" 
                                                BorderBrush="{TemplateBinding BorderBrush}" 
                                                BorderThickness="{TemplateBinding BorderThickness}" 
                                                CornerRadius="12" 
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="Bg" Property="Background" Value="#FFFCE8E6"/>
                                                <Setter TargetName="Bg" Property="BorderBrush" Value="#C62828"/>
                                                <Setter Property="Foreground" Value="#C62828"/>
                                            </Trigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Bg" Property="Background" Value="{DynamicResource PrimarySoftBrush}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ToggleButton.Style>
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Kind="AlertCircleOutline" Width="16" Height="16" Margin="0,0,8,0"/>
                        <TextBlock Text="Solo Críticos"/>
                    </StackPanel>
                </ToggleButton>

                <Button Grid.Column="2" Command="{Binding BuscarCommand}" Style="{StaticResource PrimaryButton}">
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Kind="Refresh" Width="16" Height="16" Margin="0,0,8,0"/>
                        <TextBlock Text="Actualizar"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <!-- Grid -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Items}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  RowStyle="{StaticResource StockRowStyle}"
                  Background="White"
                  BorderThickness="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Código" Binding="{Binding Codigo}" Width="120"/>
                <DataGridTextColumn Header="Producto" Binding="{Binding Nombre}" Width="*"/>
                <DataGridTextColumn Header="Unidad" Binding="{Binding Unidad}" Width="100"/>
                <DataGridTextColumn Header="Stock Actual" Binding="{Binding StockActual}" Width="120" FontWeight="Bold"/>
                <DataGridTextColumn Header="Mínimo" Binding="{Binding NivelMinimo}" Width="100"/>
                <DataGridTextColumn Header="Máximo" Binding="{Binding NivelMaximo}" Width="100"/>
                <DataGridTemplateColumn Header="Estado" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border CornerRadius="4" Padding="8,2" HorizontalAlignment="Center">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#E0E0E0"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding NivelEstado}" Value="Critico">
                                                <Setter Property="Background" Value="#EF5350"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding NivelEstado}" Value="Bajo">
                                                <Setter Property="Background" Value="#FFCA28"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding NivelEstado}" Value="Seguro">
                                                <Setter Property="Background" Value="#66BB6A"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="{Binding NivelEstado}" Foreground="White" FontSize="11" FontWeight="SemiBold"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Loading -->
        <Border Grid.RowSpan="3" Background="#80FFFFFF" Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="20"/>
                <TextBlock Text="Cargando..." Margin="0,8,0,0" HorizontalAlignment="Center" Foreground="{StaticResource MutedBrush}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
